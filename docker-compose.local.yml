services:
  postgres:
    image: postgres:17-alpine
    container_name: corelybase_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-corelybase}
      POSTGRES_USER: ${POSTGRES_USER:-corelybase}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-corelybase}
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-corelybase}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres_data:
